import s from './Footer.module.css';

const Footer = () => (
  <footer className={s.root}>
    <div className={s.container}>
      <span className={s.text}>Made with ❤️ for developers</span>
    </div>
  </footer>
);

export default Footer;
